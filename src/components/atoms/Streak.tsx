import React from 'react'
import styled from '../../utils/styled'
import IconFire from './IconFire'

const Container = styled.p`
  font-size: 2rem;
  margin-left: 1.5rem;
`

const Streak = () => <Container>1 Day Streak</Container>

export default Streak
